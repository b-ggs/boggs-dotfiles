#/usr/bin/perl

my $full_text;
my $mute_status;

open(MUTE, 'amixer -D pulse get Master | egrep \'Playback.*?\\[o\' | egrep -o \'\[off\]\' -m 1') or die "Can't exec amixer";
$mute_status = <MUTE>;
close(MUTE)

if ($mute_status eq '[off]') 
  $full_text .= "off";

print "$full_test";
